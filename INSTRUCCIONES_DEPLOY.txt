# Instrucciones de Despliegue - KPIs Rosti (Sencillo y Definitivo)

## 1. Copiar Archivos
Copie **toda la carpeta del proyecto** (`CalendarioPresupuesto`) al servidor de destino. (Sobrescriba lo que haya).

## 2. Iniciar Sistema
Dentro de la carpeta copiada, haga doble clic en el nuevo archivo:

👉 **`INICIAR_SISTEMA_V2.bat`**

Este es un **super script** que hace todo:
1.  **Configura las credenciales** automáticamente (crea el archivo `.env`).
2.  **Limpia la versión vieja** de la página web.
3.  **Construye la nueva versión** desde cero.
4.  **Inicia el servidor** y abre el navegador.

---
**Nota Importante:** La primera vez que lo corras (o cuando copies cambios nuevos), tardará unos minutos en el paso 4 ("CONSTRUYENDO NUEVA VERSION WEB"). ¡Ten paciencia! Es necesario para que se vean los cambios.
